This private (for the moment) repository contains my .emacs scripts.

To install,
```
ln -s $PWD/dot-emacs ~/.emacs
ln -s $PWD/dot-emacs-ada.el ~/.emacs-ada.el
ln -s $PWD/dot-emacs-custom.el ~/.emacs-custom.el
```
